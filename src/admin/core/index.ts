// Admin Core Exports - Export what actually exists
// Dashboard components are in components/dashboard
// Layout components are in components/layout  
// Shared components are in components/shared

// For now, just export the main dashboard
export { AdminDashboard } from '../AdminDashboard';
